Simple hello
  $ cpcc simple_hello.cpc
  Printnl(Str(Hello friends :^)))
  Printnl(Str(All your code base are belong to us !))

Simple variables assign
  $ cpcc define_variables.cpc
  Let(pi, Float(3.14159))
  Let(hello, Str(friends :^)))
  Let(x, Int(2))
